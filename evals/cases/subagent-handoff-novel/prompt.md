A PR has been selected for repeated monitoring, and the handled-comment snapshot is ready. The runtime advertises an agent called `reviewer`, but that agent can read only the local checkout and cannot call the GitHub provider. It also offers a generic persistent agent with read-only repository and provider access. The draft selects `reviewer` because of its name and asks it to resolve threads and push fixes. Review the helper choice and handoff boundary. The lead model is already selected. Do not start polling, call a provider, edit files, or launch an agent.
