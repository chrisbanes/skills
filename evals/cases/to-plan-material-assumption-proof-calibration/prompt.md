Create a local implementation plan for this repository. Planning only: do not run the proof, edit source files, or contact a provider. The task is to publish generated reports to two configured target-runtime mounts. The proposed implementation writes a temporary report on the first mount and atomically renames it to the second mount before marking publication complete. A failed rename would invalidate that approach and could leave reports unpublished. Repository inspection cannot establish whether the runtime mounts share a filesystem. Plan a bounded early-proof task that tests the assumption on the target runtime, then gates the dependent publication implementation on its result. State the hypothesis, exact setup, observable pass/fail condition, and stop/re-plan behavior for failure. Use the supplied fixture's local planning boundary and write one executable plan now.
