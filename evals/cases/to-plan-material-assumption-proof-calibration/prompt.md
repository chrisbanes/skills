Create a local implementation plan for this repository. Planning only: do not run the proof, edit source files, or contact a provider. The task is to change report publication across two configured target-runtime mounts. The proposed behavior writes a temporary report on the staging mount and uses `os.replace(staged_path, published_path)` to atomically rename it to the published mount before marking publication complete. A failed rename would invalidate that approach and could leave reports unpublished. Repository inspection cannot establish whether the runtime mounts share a filesystem. Inspect the existing report-publisher code and mount configuration; ground your plan in the actual files, symbols, current behavior, and configured values rather than inventing them. Distinguish the current publisher behavior from the proposed post-proof change: identify the existing direct `published.write_bytes(contents)` followed by `mark_published(report_id)`, and describe `os.replace` only as the proposed post-proof operation. Plan a bounded early-proof task that tests the assumption on the target runtime, then gates the publisher change on its result. State the hypothesis, exact setup, observable pass/fail condition, and stop/re-plan behavior for failure. Use the supplied fixture's local planning boundary and write one executable plan now. Use the skill template's task dependency syntax with each task ID separately backticked. T2 must depend on T1. T3 must depend directly on T1, as well as T2, so its task metadata names the proof prerequisite and the implementation prerequisite.
