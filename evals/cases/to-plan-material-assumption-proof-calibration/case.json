{
  "id": "to-plan-material-assumption-proof-calibration",
  "title": "Gate implementation on a target-runtime proof",
  "family": "workflow",
  "target_skills": ["to-plan"],
  "expected_skills": ["to-plan"],
  "task_mode": "edit",
  "kind": "direct",
  "fixture": "workflow-plan",
  "allowed_write_paths": [".scratch/to-plan"],
  "forbidden_command_patterns": ["\\b(?:gh|git)\\s+(?:api|issue|pr|project|push|fetch|pull)\\b"],
  "validators": [
    {
      "argv": ["python3", "@validators/text_case.py", "to-plan-material-assumption-proof-calibration"],
      "timeout_seconds": 30
    }
  ],
  "rubric": [
    {
      "id": "bounded-proof",
      "text": "Plans a first bounded target-runtime proof with the hypothesis that an atomic rename works between the two publication mounts, setup that creates a small file on the source mount and attempts the rename into the destination mount, and an observable pass condition that the destination contains the exact bytes and the source is gone"
    },
    {
      "id": "failure-gate",
      "text": "Makes proof failure stop the dependent publication implementation and trigger re-planning; does not treat a failed cross-mount rename as an implementation detail to work around during the dependent slice"
    },
    {
      "id": "task-dependency",
      "text": "Declares the proof as T1 and the dependent implementation as T2, with T2 depending on T1, in an acyclic task graph"
    },
    {
      "id": "planning-boundary",
      "text": "Writes only a local plan and does not run the target-runtime proof, edit source, or contact a provider"
    }
  ],
  "provenance": {"kind": "synthetic"},
  "calibration": true
}
