A supplied plan has stable task IDs and this chain: `SCHEMA` creates a shared schema; `WRITER` adds a producer and depends on `SCHEMA`; `READER` adds a consumer and depends on `WRITER`. Each task modifies the same module file because later edits rely on prior code. The runtime has several implementation agents and supports isolated worktrees. Review the dispatch order and acceptance gates. Preserve the schema dependency and same-owner repair contract. Do not start agents or install anything.
