Create a local implementation plan for this repository. Do not edit source or
test files or contact a provider. Both `manifest_reader.py:missing_manifest_error`
and `profile_loader.py:invalid_profile_error` include a supplied path in a
diagnostic. Change the diagnostics so the path is enclosed in single quotes,
while preserving each prefix, public function signature, and path text. For
example, preserve the space in `configs/team manifest.json` inside the quotes.

Use the inspected repository state and fixture instructions to plan the work.
Write the plan now.
