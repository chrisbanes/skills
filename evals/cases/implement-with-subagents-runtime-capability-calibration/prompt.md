Review implementation-owner capability in four independent runtimes. Claude Code exposes built-in `general-purpose` agents. OpenCode exposes a `general` subagent and a primary `build` agent. Bare Pi has no delegation extension or subagent package installed. In a fourth environment, Pi has an extension exposing an editing agent named `worker`, but each call starts a fresh session and cannot resume. The required `implement`, `tdd`, and `code-review` skills are available in every environment. State the owner choice or stop condition for each runtime and whether the Pi extension can preserve same-owner repair responsibility. Do not start agents, install anything, edit files, or contact a provider.
